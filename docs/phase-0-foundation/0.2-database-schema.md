Step 0.2 - Database Schema Design
Phase: 0 - Foundation & Planning
Duration: 1 hour
Status: In Progress

Database Technology
Primary Database: Firebase Firestore (NoSQL)

Real-time sync
Offline support built-in
Scalable
Easy integration with React Native

Offline Database: SQLite (via expo-sqlite)

Local caching
Works without internet
Syncs when online


Collections & Schema
1. Users Collection
Collection Name: users
javascript{
  userId: "string (auto-generated)",
  email: "string",
  displayName: "string",
  profilePhoto: "string (URL)",
  createdAt: "timestamp",
  
  // Verification
  isVerified: "boolean",
  verificationMethod: "string (google, email, social)",
  verifiedTrips: ["array of trip receipts/photos"],
  
  // Points & Gamification
  totalPoints: "number",
  currentLevel: "string (Beginner Traveler, Local Explorer, City Guide, Global Nomad)",
  pointsHistory: [{
    action: "string",
    points: "number",
    timestamp: "timestamp"
  }],
  achievements: ["array of achievement IDs"],
  
  // Preferences
  preferences: {
    budgetRange: "string (low, medium, high)",
    travelStyle: "string (backpacker, solo, family, etc.)",
    language: "string",
    currency: "string"
  },
  
  // Stats
  stats: {
    locationsAdded: "number",
    reviewsWritten: "number",
    upvotesReceived: "number",
    helpfulVotes: "number"
  }
}

2. Locations Collection
Collection Name: locations
javascript{
  locationId: "string (auto-generated)",
  type: "string (restroom, parking, restaurant, hostel, attraction, atm)",
  
  // Basic Info
  name: "string",
  description: "string",
  address: "string",
  city: "string",
  country: "string",
  
  // Coordinates
  coordinates: {
    latitude: "number",
    longitude: "number"
  },
  
  // Pricing
  pricing: {
    priceLevel: "string (free, $, $$, $$$)",
    exactPrice: "number (optional)",
    currency: "string"
  },
  
  // Availability
  hours: {
    monday: {open: "time", close: "time"},
    tuesday: {open: "time", close: "time"},
    // ... rest of week
  },
  isOpen24Hours: "boolean",
  
  // Features (for restrooms)
  features: {
    isAccessible: "boolean",
    hasChangingTable: "boolean",
    requiresKey: "boolean",
    isFree: "boolean",
    cleanliness: "number (1-5 average)"
  },
  
  // For parking
  parkingInfo: {
    spotsAvailable: "number",
    maxHeight: "number (meters)",
    isSecure: "boolean",
    hasEVCharging: "boolean"
  },
  
  // Photos
  photos: ["array of photo URLs"],
  
  // User Engagement
  addedBy: "userId",
  addedAt: "timestamp",
  lastVerifiedAt: "timestamp",
  verifiedBy: ["array of userIds who verified"],
  
  // Ratings
  averageRating: "number (1-5)",
  totalReviews: "number",
  upvotes: "number",
  downvotes: "number",
  
  // Status
  isActive: "boolean",
  reportedIssues: ["array of issue reports"],
  lastUpdated: "timestamp"
}

3. Reviews Collection
Collection Name: reviews
javascript{
  reviewId: "string (auto-generated)",
  locationId: "string (reference to location)",
  userId: "string (reference to user)",
  
  // Review Content
  rating: "number (1-5)",
  title: "string",
  text: "string",
  photos: ["array of photo URLs"],
  
  // Verification
  isVerifiedVisit: "boolean",
  visitDate: "timestamp",
  visitProof: "string (photo URL or receipt)",
  
  // Engagement
  upvotes: "number",
  downvotes: "number",
  helpfulCount: "number",
  
  // Moderation
  isFlagged: "boolean",
  flagReasons: ["array of strings"],
  isHidden: "boolean",
  moderatedBy: "userId (admin)",
  
  // Timestamps
  createdAt: "timestamp",
  updatedAt: "timestamp"
}

4. Points & Achievements
Collection Name: achievements
javascript{
  achievementId: "string",
  name: "string",
  description: "string",
  icon: "string (emoji or icon name)",
  pointsRequired: "number",
  category: "string (contributor, explorer, helper, etc.)",
  
  // Rewards
  perks: ["array of perk descriptions"],
  badgeUrl: "string (URL to badge image)"
}
Point Actions (stored in code, not database):
javascriptconst POINT_VALUES = {
  ADD_RESTROOM: 10,
  ADD_PARKING: 10,
  VERIFY_LOCATION: 5,
  WRITE_REVIEW: 5,
  UPVOTE_REVIEW: 2,
  DAILY_LOGIN: 3,
  REFER_FRIEND: 20,
  PHOTO_UPLOAD: 3
}

5. Trips Collection
Collection Name: trips
javascript{
  tripId: "string (auto-generated)",
  userId: "string (reference to user)",
  
  // Trip Info
  name: "string",
  destination: "string",
  startDate: "timestamp",
  endDate: "timestamp",
  
  // Budget
  totalBudget: "number",
  spentAmount: "number",
  currency: "string",
  
  // Saved Locations
  savedLocations: [{
    locationId: "string",
    category: "string (to_visit, visited, favorite)",
    notes: "string",
    addedAt: "timestamp"
  }],
  
  // Itinerary (AI-generated or manual)
  itinerary: [{
    day: "number",
    activities: [{
      time: "string",
      locationId: "string",
      description: "string",
      estimatedCost: "number"
    }]
  }],
  
  // Sharing
  isPublic: "boolean",
  sharedWith: ["array of userIds"],
  
  createdAt: "timestamp",
  updatedAt: "timestamp"
}

6. Messages Collection (Traveler-to-Traveler)
Collection Name: messages
javascript{
  messageId: "string (auto-generated)",
  conversationId: "string (thread identifier)",
  
  // Participants
  senderId: "string (userId)",
  recipientId: "string (userId or 'anonymous')",
  
  // Message
  text: "string",
  locationId: "string (optional, if asking about specific location)",
  
  // Privacy
  isAnonymous: "boolean",
  
  // Moderation
  isFlagged: "boolean",
  isHidden: "boolean",
  
  createdAt: "timestamp",
  readAt: "timestamp"
}

7. Budget Tracker Collection
Collection Name: expenses
javascript{
  expenseId: "string (auto-generated)",
  userId: "string",
  tripId: "string (optional)",
  
  // Expense Details
  category: "string (food, transport, accommodation, activities, other)",
  amount: "number",
  currency: "string",
  description: "string",
  locationId: "string (optional)",
  
  // Receipt
  receiptPhoto: "string (URL)",
  
  date: "timestamp",
  createdAt: "timestamp"
}

8. Cultural Info Collection
Collection Name: culturalInfo
javascript{
  culturalInfoId: "string (auto-generated)",
  country: "string",
  city: "string (optional)",
  
  // Language
  commonPhrases: [{
    english: "string",
    local: "string",
    pronunciation: "string"
  }],
  
  // Currency
  currency: "string",
  exchangeRate: "number (to USD)",
  lastUpdated: "timestamp",
  
  // Customs
  culturalTips: ["array of strings"],
  dosDonts: {
    dos: ["array of strings"],
    donts: ["array of strings"]
  },
  
  // Safety
  safetyTips: ["array of strings"],
  emergencyNumbers: {
    police: "string",
    ambulance: "string",
    fire: "string"
  }
}

9. Moderation Queue Collection
Collection Name: moderationQueue
javascript{
  queueId: "string (auto-generated)",
  type: "string (review, message, location, user)",
  itemId: "string (reference to flagged item)",
  
  // Flag Details
  flaggedBy: "userId",
  reason: "string",
  description: "string",
  
  // AI Analysis
  aiScore: "number (0-1, likelihood of violation)",
  aiReason: "string",
  
  // Status
  status: "string (pending, reviewed, resolved, dismissed)",
  reviewedBy: "userId (admin)",
  action: "string (hide, warn, ban, approve)",
  
  createdAt: "timestamp",
  resolvedAt: "timestamp"
}

Database Relationships
Users (1) -----> (Many) Locations
Users (1) -----> (Many) Reviews
Users (1) -----> (Many) Trips
Users (1) -----> (Many) Expenses
Users (1) -----> (Many) Messages

Locations (1) -----> (Many) Reviews
Trips (1) -----> (Many) Saved Locations
Trips (1) -----> (Many) Expenses

Reviews (Many) -----> (1) Location
Reviews (Many) -----> (1) User

Indexes for Performance
Firestore Composite Indexes Needed:

Locations by City & Type:

   Collection: locations
   Fields: city (Ascending), type (Ascending), averageRating (Descending)

Reviews by Location:

   Collection: reviews
   Fields: locationId (Ascending), createdAt (Descending)

User Points History:

   Collection: users
   Fields: userId (Ascending), pointsHistory.timestamp (Descending)

Active Trips:

   Collection: trips
   Fields: userId (Ascending), endDate (Descending)

Data Validation Rules
Firebase Security Rules (Example):
javascriptrules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Users can only read/write their own data
    match /users/{userId} {
      allow read: if request.auth != null;
      allow write: if request.auth.uid == userId;
    }
    
    // Anyone can read locations, only verified users can write
    match /locations/{locationId} {
      allow read: if true;
      allow create: if request.auth != null && 
                       get(/databases/$(database)/documents/users/$(request.auth.uid)).data.isVerified == true;
      allow update: if request.auth != null;
    }
    
    // Reviews must be from authenticated users
    match /reviews/{reviewId} {
      allow read: if true;
      allow create: if request.auth != null && 
                       request.resource.data.userId == request.auth.uid;
      allow update, delete: if request.auth.uid == resource.data.userId;
    }
  }
}

Sample Data Structure Examples
Example User:
json{
  "userId": "user123",
  "email": "hamza@example.com",
  "displayName": "Hamza",
  "totalPoints": 142,
  "currentLevel": "Local Explorer",
  "stats": {
    "locationsAdded": 8,
    "reviewsWritten": 12,
    "upvotesReceived": 45
  }
}
Example Location (Restroom):
json{
  "locationId": "loc456",
  "type": "restroom",
  "name": "Central Park Public Restroom",
  "coordinates": {
    "latitude": 40.7829,
    "longitude": -73.9654
  },
  "pricing": {
    "priceLevel": "free",
    "isFree": true
  },
  "features": {
    "isAccessible": true,
    "hasChangingTable": true,
    "cleanliness": 4.2
  },
  "averageRating": 4.5,
  "totalReviews": 28
}

Next Steps
After completing this schema:

Review and adjust based on feature requirements
Create Firestore collections in Firebase Console
Set up security rules
Create sample data for testing


Step 0.2 Status: Complete
Next Step: 0.3 - UI/UX Design System
Last Updated: January 17, 2025
